/**
 * Used to seperate normal and kill moves.
 */
public enum MoveType
{
    /**
    * Used for normal moves.
    */
    NORMAL, 
    /**
    * Used for moves with a kill included.
    */
    KILL
}
